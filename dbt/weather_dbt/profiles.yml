weather_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'localhost') }}"
      port: 5432
      user: weather
      password: weather_pipeline_2026
      dbname: weather_warehouse
      schema: staging
      threads: 4
